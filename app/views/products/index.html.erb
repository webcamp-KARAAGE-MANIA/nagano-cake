<div class="container cart">
  <div class="row">
    <div class="col_md_12 bg-light">
      <h4>商品一覧<span>(全<%= @products.count %>件)</span></h4>
    </div>
    <!--<div class="d-flex ml-auto">-->
    <!--  <select class="form-select" aria-label="Disabled select example" disabled>-->
    <!--    <option selected>ジャンル検索</option>-->
    <!--    <%# @genres.each do |genre| %>-->
    <!--      <option>-->
    <!--        <%#= link_to search_search_path('search[value]': genre.id, 'search[how]': "match"),style: "color: black;" do %>-->
    <!--        <%#= genre.name %>-->
    <!--        <%# end %>-->
    <!--      </option>-->
    <!--    <%# end %>-->
    <!--　</select>-->
    <!--</div>-->
  <!--</div>-->
  </div>
      <div class="row">
        <div class="col_md_2">
          <table class="table table-bordered genre-table">
            <thead class="thead-light">
              <tr>
                <th>ジャンル検索</th>
              </tr>
            </thead>
            <tbody>
            <% @genres.each do |genre| %>
              <tr>
                <td>
                  <%= link_to genre.name, search_search_path('search[value]': genre.id, 'search[how]': "match") %>
                </td>
              </tr>
            <% end %>
            </tbody>
        </table>
        </div>
         <div class="col-md-10">
           <div class="d-flex flex-wrap mt-2">
            <% @products.each do |product| %>
                 <%= link_to product_path(product.id),style: "color: black;" do %>
                  <%= attachment_image_tag product, :image, fallback:"no-image.png", size: '180x130' %>
                    <div><%= product.name %></div>
                    <div>¥<%= product.add_tax_price.to_s(:delimited) %></div>
                    <div><% if product.is_active == true %>
                     <p class="text-success">販売中</p>
                     <% else %>
                     <p class="text-black-50">売り切れ</p>
                      <% end %>
                    </div>
                　<% end %>
          　<% end %>
           </div>
        </div>
    </div>
     <div class="col align-self-center"><%= paginate @products%></div>
  </div>