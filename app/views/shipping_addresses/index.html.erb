<div class="container">
  <div class="row">
    <div class="bg-light pl-4 pr-4 text-center">
      <h5 class="lh-auto text-center">配送先登録/一覧</h5>
    </div>
  </div>
  <div class="row mt-4">
    <div class="form-group w-auto mx-auto col-md-8">
      <%= form_with model: @shipping_address, local:true do |f| %>
        <%= f.label :postal_code, '郵便番号(ハイフンなし)', class:"col-3" %>
        <%= f.text_field :postal_code, placeholder: "0000000", class:'col-5' %>
        <%= f.label :address, '住所', class:"col-3" %>
        <%= f.text_field :address, placeholder: "東京都渋谷区代々木神園町0-0", class:'col-5' %>
        <%= f.label :attention, '宛名', class:"col-3" %>
        <%= f.text_field :attention, placeholder: "唐揚マニオ", class:'col-5' %>
        <%= f.submit '新規登録' %>
      <% end %>
    </div>
  </div>
  <div class="row">
    <table class="table">
      <thead>
        <tr>
          <td>郵便番号</td>
          <td>住所</td>
          <td>宛名</td>
          <td colspan="1"></td>
        </tr>
      </thead>
      <tbody>
        <% @shipping_addresses.each do |shipping_address| %>
          <tr>
            <td><%= shipping_address.postal_code %></td>
            <td><%= shipping_address.address %></td>
            <td><%= shipping_address.attention %></td>
            <td>
              <%= link_to "編集する", edit_shipping_address_path(shipping_address.id), class:'btn btn-success' %>
              <%= link_to "削除する", shipping_address_path(shipping_address.id), method: :delete, class:'btn btn-danger' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
